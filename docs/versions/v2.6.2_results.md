# v2.6.2 — Results

Single seed (42), closed single-seed by decision: the config is not final (v2.7.0 changes the
observation), so escalation is deferred to the next verdict-grade config per the seed-economy rule. This
version has three substantive outputs: (1) the situational velocity objective, validated in its
braking-envelope form; (2) the IC-exclusion question, closed with evidence (full-range stays official);
(3) the gravity-observability discovery that defines v2.7.0.

## 1. Result

One mechanism (situation-dependent velocity objective) in `policy_bptt_loss`, landed in two amendments:
amendment 1 reverted the mis-motivated Huber goal-distance (quadratic restored); amendment 2 replaced the
inert gaussian-gated approach term with the braking envelope
$w_a \sum_k \mathrm{relu}(s_k \tau_b - d_k)^2$ ($w_a{=}30$, $\tau_b{=}0.6$), after the first (gaussian)
run REGRESSED collision. Full pool `eval_full_quadrotor-planar_n2000_seed23456`, value/baseline reused
from v2.6.1 M1 (sha 1099eaf…):

| run (best.pt) | cps [95% CI] | reach | collision [CI] | timeout |
|---|---|---|---|---|
| **brake-envelope (final)** @30000, sha b33e678… | **0.7311 [0.6925, 0.7648]** | 0.9050 | 0.0670 [0.0565, 0.0780] | 0.0250 |
| gaussian FULL (superseded within-version) | 0.6097 | 0.8535 | 0.0955 | 0.0510 |
| v2.6.1 M6 (reference, same plant/pool) | 0.5753 [0.533, 0.616] | 0.8205 | 0.0810 | 0.0985 |

Insertion variants: LQR-only −0.383, frozen 0.665, live 0.679. P3 CONFIRMED: 123 residual collisions,
0/123 collinear, ‖L_g V̂‖ median 0.453 — the certificate is never the collision culprit.
Mechanism checks, reported MIXED as measured: inward speed at the 1.0 m crossing 1.785 (below the
gaussian's 1.94, not below v2.6.1's 1.66); envelope occupancy near obstacles 0.124; residual collisions
remain 100% filter-infeasible at impact while their count fell 159 → 123 (−23%). The envelope cut
collision by reducing fast-approach commits and overall speed, not by making committed approaches
feasible.

## 2. Versus motivation

The opening hypotheses resolved as follows (single seed):
- **H-goal (settling) CONFIRMED:** timeout 0.0985 → 0.0250 (−75%), reach 0.8205 → 0.9050; the v2.6.1
  early-stuck transient did not recur (the sparse ‖v_T‖ terminal's global-damper role was retired,
  w_terminal_v = 0).
- **H-appr CONFIRMED in the amendment-2 form only:** collision 0.0670, CI entirely below v2.6.1's 0.0810
  (−17%), fixing the gaussian regression (0.0955, +18%). The gaussian gate failed for the diagnosed
  reasons — gate ≈ 0.018 at 1.0 m surface (past the ~0.9–1.0 m point-of-no-return) and term p90 0.84 vs
  the goal quadratic's 30.4 (~36×) — so the component was inert as first calibrated; the braking
  envelope (earlier-the-faster engagement, weight at the goal-term scale) is what made it act.
- **Combined:** cps 0.7311, +0.156 over v2.6.1, CI-SEPARATED (v2.6.1-hi 0.616 < lo 0.6925); +0.121 over
  the within-version gaussian run.

## 3. Improvement verdict

**Improved** (single-seed, CI-separated, same plant and pool as v2.6.1 — a direct comparison). The
brake-envelope row is the (v2.6.2, quadrotor_planar) full-range baseline, SOTA-bold in the ledger.
Caveats: single seed; mechanism checks mixed (the residual failure mode is unchanged in kind, reduced in
frequency); the certified-metrics manifest of the frozen pool carries stale sampler metadata
(v_init_max/tau_max fields predate the pool's actual contents — errata recorded; the pool itself is
frozen, sha-matched, and used consistently, so comparability is unaffected).

## 4. Next axis (the version's real output)

Three findings define what follows:
- **The IC-exclusion line is CLOSED with evidence — full-range is the official distribution.** Residual
  anatomy: of 123 collisions, 72 "born-doomed" (V̂-infeasible at t=0, concentrated |θ₀|>π/2 +
  fast-inward), 41 early-, 10 late-trapped. Every exclusion criterion failed or was rejected: the 1-D
  kinematic test structurally over-fires (FP 0.21; 66.5% of real escapes are tangential drift-past);
  witness-search is rejected on principle (search incompleteness would exclude solvable ICs — finding
  solutions is the JT's job); dense 6D HJ is measured infeasible (178–1661 GB). The decisive doom
  census: a 3g-relaxed ballistic closed form AND a 4D relaxed HJ both flag 0/2000 pool ICs, 0/72
  "born-doomed", 0/1807 reach ICs (zero-FP gates passed). The provable-doom set is empty: residual
  collisions are policy-suboptimality — training targets, not bad ICs. Predictive-α is closed in both
  signs (inflation permissive-refuted; deflation rescues trapped cases but RESHUFFLES — net collision
  unchanged, timeout up).
- **Gravity-observability (defines v2.7.0).** The dim-20 body-frame observation drops θ — an SE(2)
  quotient inherited from the unicycle — while gravity breaks rotational symmetry (the closed-loop
  symmetry group is translations only). Diagnostic: the active thrust-misfire mechanism is DISCONFIRMED
  (high-tilt collisions are drift / failure-to-recover, misfire 0.037); the representational blindness is
  CONFIRMED — θ undecodable exactly in the failure cell (98.6° error at high-tilt+early vs 1.1° when
  gravity imprints v), exact aliasing (nets identical to 1e-6 across aliased pairs with true 1-step
  divergence Δclosure 0.42–0.54 m/s), and the certificate falsely dooms savable states (high-tilt starts
  infeasible 2× more, 16.8% vs 8.9%, yet 61.5% of those reach) — the value inherited the hover-nominal's
  recovery incompetence, a self-confirming trap.
- **v2.7.0 (next):** append (sin θ, cos θ) to the quadrotor observation (dim 20 → 22; the 01_env
  observation design rule) — a model-correctness fix grounded in the theory note's observation-sufficiency
  section, standing regardless of performance. Registered conservatively: the structural checks
  (decodability, aliasing, the false-doom shrink over JT) are near-certain; the performance gain
  (tilted-band collision, cps > 0.7311) is uncertain since the misfire story was disconfirmed. Falsifier
  branch: if structure improves but collision does not move, the bottleneck is recovery TIME/CREDIT and
  the next axis is the BPTT window/credit, not more observation. Full retrain required (M1 included —
  the obs change invalidates all checkpoints); same pools (IC distribution is obs-independent), so direct
  comparability with 0.7311 is retained. Later candidates, in evidence order: viability-consistent V̂
  (only after the obs fix), recovery-capable nominal (fallback), 3D quadrotor (the equivariance lesson
  generalizes: quotient by yaw only).