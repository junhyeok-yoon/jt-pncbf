# v2.8.0 results — exact projection, angular-settling terminal, dual scoring

Single-seed version (seed 42 throughout; scene-bootstrap 95% CIs). Sources: every number below
is recorded in `docs/versions/v2.8.0/close_retrieve.md` with its artifact path; key sidecars are
cited inline.

## 1. Result

**Deliverable** — dual arm, `best.pt` step 42000 (sha `cf948104`), canonical pool
`eval_full_…_n2000_seed23456` (`0ef3751b`), new terminal (ω_G=0.48), shipped fallback
(`s3_eval/m4_dual.json`):

| cps | reach | collision (obst/floor/ceil) | stuck | timeout | infeas |
|---|---|---|---|---|---|
| **0.7919** [0.762, 0.823] | 0.9295 | 0.0440 (.0095/.0335/.0010) | 0.0000 | 0.0265 | 0.1211 |

Dual scoring (standalone lineage, `m3_v2.8.0_dual.json`): **cps_tilt60 0.9206** [.905, .935]
(floor .0335→.0015), **cps_bandopen 0.8818** [.859, .901] (crossing rate 0.0345; 51/69
reached after crossing). Driver lineage: 0.9230 / 0.8862 (`w4_driver_reference.json`; gap
+0.0024/+0.0044 = the measured cross-lineage band, `r1/cluster_report.json`: 168 episode flips,
within-lineage spread 0). Enum arm (S4, pre-crash step 33000, sha `3fd4293e`): cps 0.7778
[.747, .809]; its 50k endpoint comparison is **comparison unavailable** (cold-buffer resume).

## 2. Versus motivation

**Axis (exact projection, `dual_solve`).** Registered as premise restoration with no cps
prediction. Measured: the deployed enumeration was a projection in **no** dimension (two
independent mechanisms — candidate-family omission for m≥3, `prop:enum-exact`(b); regularizer
self-defeat for m≤2, `prop:eps-selection`; 15.67% of feasible rows wrong at m=4, 5–13%
divergence at m≤2). The repair is exact and gradient-carrying everywhere (`cor:lambda-jac`).
Training channel: paired at the matched-budget common step 34500 (`p3_paired.json`), Δcps full
+0.0141 [−, CI crosses 0], hold-feasible **−0.0321 [−0.0645, −0.0028]** — the only separated
signal favors enumeration. Deployment channel (M5): −0.0013, inside the band. Command quality:
TV/s log-log slope **0.124±0.033 (dual) vs 0.531±0.069 (enum)** (theory 0 vs 1,
`prop:tv-rate`); 4.8× at 500 Hz; inter-sample barrier decay −1.2 common to both. Lag (D4):
**unscored** — the relaunched fresh-process τ-sweep did not complete (`data/runs/v2.8.0/c5/`: 4/30
cells; the four τ=0 inertness cells bit-match their recorded deploy-rate rows, but the 500 Hz τ=0
pair and every τ>0 cell are `not recorded`, and no `c5_summary.json` / falsifier artifact exists),
so the prediction (enumeration's collision rate rises faster than `dual_solve`'s as τ grows) stands
**untested** — flagged `rem:lag-null` for Researcher decision.

**Terminal (ω_G=0.48, ratio-matched; equalized settling).** T1 reclassification on the untrained
checkpoint confirmed (reach 0.933→0.194, stuck 0→0.7295 — definition artifact). After
retraining, **T2 MET: reach 0.9295 ≥ 0.8875**; matched-fallback 2×2 isolates the pure terminal
cost at **−0.006** (registered −0.008 folded ~0.002 of fallback change). Angular settling is
learnable at unchanged budget and nearly free. (Sidecar keys read `T1_3d`; the registered name
T2 is used here — recorded discrepancy (2).)

**Dual scoring (permanent standard).** Two columns installed (`cps_tilt60`, `cps_bandopen`,
ledger 18-col schema, R-audit 0 violations / 211 rows); the definitions are not
rank-equivalent — `cps_bandopen` promotes floor-dominant checkpoints (A3) — and are read as a
pair. Floor bracket (`floor_feasibility.json`): unrecoverable 1 / demonstrated-recoverable 1852
/ undetermined 147; the deliverable's 67 floor collisions include **0** from
demonstrated-recoverable ICs — the floor is an IC property on this pool (`prop:floor-bracket`).

## 3. Improvement verdict

**No improvement detected** (single-seed). The headline is not directly comparable to the
previous secured baseline (`09c33bf4`, old terminal, canonical-with-z 0.8051): the scoring
definition changed this version. Under matched conditions the checkpoint-quality change is
within the equivalence band (pure terminal −0.006; projection training effect null-to-adverse
at 34500). The version's yield is correctness and regularity, not score: the filter is now an
exact projection with restored gradients (zero-Jacobian 55%→structural classes only), command
TV is rate-bounded (deployability), the terminal now encodes settling, and the scoring standard
separates navigation from full-attitude capability. 3-D bold remains vacant (0 bold among 31
v2.8.0 rows); bold and secured promotion are Researcher decisions carried in the close
transmittal.

## 4. Next axis

v2.8.1 (Researcher-decided, two stages): Stage 1 — ω_G 0.30 / w_settle_ang 1.0 + soft top-k
encoder (new lineage); Stage 2 — smoothness on the Stage-1 checkpoint: empty-branch
continuization (`prop:empty-prox`), infeasibility margin cost (`prop:sinfty-decomp`,
`rem:sinfty-signal`; reopened by mechanism removal), policy rate term; arms A(①)/B(①+②)/
C(①+②+③). Deferred: dt randomization (v2.8.2+, gated on a disturbance rate-sweep), encoder
relabel weighting, conditional-clearance pool (κ-arrest rejection sampling; 4-D HJ optional).

## Open discrepancies at close (from close_retrieve.md R7)
(1) C5 inertness recorded 4/6 with D4 grid unscored on the retrieved path — resolved by the
close-execute D4 module; (2) `T1_3d` sidecar keys vs registered T2; (3) theory.tex 3510 vs 3511
lines (off-by-one; all 7 labels present); (4) `m4_enum.json` null step/ckpt fields (provenance
gap, values match S4); (5) HEAD at v2.7.7 with strings at v2.8.0 (expected mid-close).