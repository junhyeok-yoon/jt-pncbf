# v2.7.1 — results.md (close verdict)

Axis: corridor-cell state injection (registered) + three Researcher-directed eval-only
iterations (Stage-1/1b/1c: k-step empty-branch fallback). Seed 42 for all quadrotor rows
(unicycle/DI rows carry their own seeds, seed 42 each); SINGLE-SEED close — multi-seed
escalation is explicitly DEFERRED to the adopted-config pipeline (open decision recorded per
§2.5). Canonical path = run_full evaluate() batch-2000; all numbers disk-verified in
`docs/versions/v2.7.1/retrieve_close_facts.md` (RCF).

## 1. Headline verdict (SOTA, single-seed) — bold moves to the adopted config + fallback

**iter-5 checkpoint (sha8 3b27d691; continuing collector, inject 0) + empty-branch fallback
(mode=kstep, k=5) is the new (quadrotor_planar) SOTA-bold row, superseding L115.**
- cps **0.9036** [0.8807, 0.9248]; collision **0.0215** [0.0155, 0.0280]; full breakdown
  (reach 0.9655 / oob 0.0000 / stuck 0.0005 / timeout 0.0125 / inf 0.0406).
- vs its own mode=none (0.8343 / 0.0410): collision CI-separated (Stage-1b); vs L115
  (0.8232 [0.7910, 0.8523] / 0.0455 [0.0370, 0.0550]): CI-SEPARATED on both (cps-lo 0.8807 >
  L115-hi 0.8523; collision-hi 0.0280 < L115-lo 0.0370).
- The three k5 rows (v2.7.1+k5 0.9152 [0.8947, 0.9350], iter-5+k5, iter-1+k5
  0.9009) are mutually CI-overlapping (confirmed on cps and collision); the headline therefore goes to the
  SIMPLEST configuration carrying no refuted mechanism — the adopted going-forward config.
  v2.7.1+k5 is the point-highest row and is NOT bold (carries the refuted injection; same
  rule as the iter-2 precedent). No injection interaction contribution: v2.7.1+k5 vs
  iter-5+k5 overlap (confirmed).
- Fallback fires on empty-intersection rows only (~5.7% of steps on this pool); the
  infeasibility flag and metric are unchanged by construction (flag-invariance test-pinned)
  — cps comparability across all rows is preserved.

## 2. Registered axis (corridor-cell state injection): REFUTED-WITH-ASSETS

- Pre-gate passed (natural mass 0.04% / 0.08% << 5%), injection realized at 11.35% of
  episodes, but state-level visitation rose only 0.040% -> 0.071%: the cell is dynamically
  TRANSIENT — start-injection cannot raise state density materially. The lever is refuted
  with a structural ceiling (recorded in `03_train` §7 at close).
- H-perf NOT met (M5 0.8407 [0.8104, 0.8690] / 0.0410 [0.0325, 0.0500] = iter-5's
  collision). H-mech NOT reached (tilted monotone-descent share 0.985 -> 0.926; corridor
  gap persisted: p50 +0.0242 -> +0.0489, frac>0 ~0.59).
- Assets recorded: ||L_g V_hat|| at collision states 0.4915 -> 1.4481 (~3x); near-B0
  authority 5.51 -> 9.60; value chain sha8 (M2 value 45b40f18 -> JT 1d9e9b46); and the P-B'
  measurement it motivated.

## 3. The version's principal finding: k-step empty-branch fallback (Stages 1/1b/1c)

- **P-B'**: k-step improvable fraction at continuous-infeasible failure states rises
  0.535 (k=1) -> 0.761 (k=5) -> 0.958 (k=10) —
  the JT-trained certificate already encodes k-step recoverability that the first-order
  row cannot read (prop:kstep / cor:kstep-spread, proved before measurement).
- **Stage-1 (v2.7.1 ckpt)**: k=5 collision 0.0410 -> 0.0175 [0.0120, 0.0235], cps 0.9152, net -47 flips (53 fixed / 6 new, all on empty-step episodes; 5/6 new
  were improvable-yet-collided — "improvable != safe"). k=10 WORSE (64 goal->collision,
  switch-rate 0.492): non-monotonic in k; k is a commitment horizon, not a gain.
- **Stage-1b**: CI-separated collision reduction reproduces on both prior checkpoints
  (iter-5 39 fixed / 2 new; iter-1 0.0455 -> 0.0225, 49 fixed / 5 new); both mode=none regressions exact
  (0/2000). Enabler discriminator: left branch — the fallback does NOT require the
  injected certificate.
- **Stage-1c (differential control, prediction registered pre-result)**: quadrotor
  CI-separated >> unicycle point-only (0.0040 -> 0.0015, n_coll 8 -> 3, underpowered) >
  DI null (0.0090 -> 0.0085) — despite HIGHER empty rates on DI/unicycle (0.1556/0.1609
  vs quadrotor ~0.057). The dissociation pins the mechanism to first-order blindness
  (underactuation chain), not to "simulate and pick" generically.
- **k-selection honesty (binding)**: the pre-registration named k=10 primary and it
  failed; k=5 is adopted as the CI-separated pre-registered cell with an interpretable
  k=10 failure mode, NOT as a swept optimum. No optimality claim for 5; further k search
  prohibited. Chattering at k=5 (switch 0.346 -> 0.515, |du| 3.44 -> 5.51) is a recorded
  characteristic, not gated.
- **Adoption scope**: quadrotor deployed default = mode=kstep k=5; DI/unicycle stay
  mode=none (null / underpowered). Positioning: RPCBF-adjacent deploy-time mechanism;
  contribution framing = minimal recovery of the learned certificate's k-step authority
  (evidence FOR JT), never a standalone novelty or headline.

## 4. Carry-forward (registered, none auto-started)

Stage-2 (BPTT-integrated fallback): registered, NOT auto-promoted — starts only if
deploy-only limits appear in data. k=1 ablation cell: registered (cheap control). Track-B
policy credit and asymmetric regression: retained candidates behind the corridor question,
which remains open (gap frac>0 ~0.59) but no longer gates the headline. 3D extension:
deferred sequencing unchanged. Multi-seed escalation: DEFERRED, target = the §1 adopted
config.

## 5. Caveats and bookkeeping (binding)

1. Denominators per `04_eval`: canonical counts for rates; agreed sets for probes
   (partitions quoted in RCF §3/§4); batch-nondeterminism caveat unchanged.
2. Ledger: v2.7.1-related rows L121-L127 (confirmed); after this close exactly
   one bold quadrotor row (the §1 row); L115 un-bolded with the standing supersession line.
3. Discrepancy adjudications from RCF §7 are authoritative over session-quoted values
   (e.g., monotone share, P-B' k=10 fraction) — the RCF numbers are the cited ones.
4. verify.sh at close: 148 passed (confirmed). Version strings consistent at v2.7.1.
5. Secured: no NEW promotion required — the headline checkpoint (3b27d691) is already
   secured from the v2.7.0 close; the fallback is code, not a checkpoint. (Researcher may
   override in the transmittal.)
6. Theory note: close-time tex update adds the empirical-status remark for
   prop:kstep/cor:kstep-spread (three-way confirmation: P-B', Stage-1/1b outcome,
   Stage-1c differential) and the receding-horizon k-commitment consistency OPEN item;
   delivered with the close.
7. Two Stage-1b flip counts in §3 were corrected at close from RCF §4 after a
   spot-verify HALT (Strategist transcription error; RCF authoritative).
