# v2.7.2 — results.md (close verdict)

Bring-up version: quadrotor_3d (13-state quaternion, cylinders, altitude goals, 6-DOF
perturbed starts). Seed 42, SINGLE-SEED close (multi-seed escalation remains DEFERRED, now
targeted at the post-difficulty-axis configuration). All claims are lineage-internal — no
cross-dimension comparison. Canonical path = batch-2000; deployed filter mode stated per
row (04_eval §2.2). All numbers disk-verified in
`docs/versions/v2.7.2/retrieve_close_facts.md` (RCF), whose §8 discrepancy adjudications
are binding here.

## 1. Lineage headline (bold within quadrotor_3d): M6 JT

**M6 JT (best.pt @33000, sha8 4baaf031; mode=none deployed default): cps 0.9329
[0.91764, 0.94889]; collision 0.0095 [0.0055, 0.0135]; reach 0.9720; oob 0.0040; stuck
0.0005; timeout 0.0140; infeasibility 0.0352.**
- vs M4 pre-JT (nominal cascaded-PD + HardNet on the M3 value @40500, sha8 e3ab0940):
  cps 0.8481 [0.82416, 0.87233]; collision 0.0200 [0.0135, 0.0260]; infeasibility 0.1071.
- **H1 CONFIRMED — carried by cps: CI-separated, gap +0.04531** (M6-lo 0.91764 > M4-hi
  0.87233). Collision halves at the point estimate (0.0200 -> 0.0095) with CIs TOUCHING at
  the shared endpoint 0.0135 — no collision-CI separation is claimed (RCF §8.3).
- Insertion triad (unforeseen obstacle): nominal-only collision 0.414 -> frozen 0.0385 ->
  live 0.0320 (cps -0.242 / 0.8649 / 0.8751): the certificate transfers to unseen
  obstacles.
- Pools (rebuilt post-refactor, RNG-identical geometry): full sha256 fe6b3016...,
  in-loop 75759201... — these supersede the build-log's M2-cited shas (RCF §8.1); an
  erratum line is appended to the build-log at close.

## 2. H2 (observation sufficiency): CONFIRMED — with the P1 probe restated

- Evidence: **P2 tilt strata FLAT** — collision 0.0041 / 0.0121 / 0.0118 / 0.0098 and
  reach 0.984 / 0.968 / 0.972 / 0.965 across [0,30)/[30,60)/[60,90)/[90,120) deg
  (n 491/494/507/508): no attitude-aliased concentration, including past-horizontal
  starts. Plus the M1 q3 bit-exactness test (joint-yaw invariance; tilt non-invariance).
- **P1 restated (RCF §8.2):** as implemented, P1 probes the value ensemble member-0
  PENULTIMATE features (not obs): linear decoding of the g^b block gives R^2
  [0.806, 0.828, 0.857] (mean 0.83) — a learned-representation retention statistic, NOT an
  obs-sufficiency test (a raw-obs probe would be ~1.0 trivially since g^b is a literal
  block). The changes.md "expect ~exact" wording specified the raw-obs probe; the
  implemented feature probe is recorded as a probe-definition deviation whose result is
  kept as an auxiliary representation datum. H2 rests on P2 + q3, not on P1.

## 3. Bring-up facts (registrations, gates, reconciliations)

- Registrations: nominal = cascaded hover-PD (approved deviation, `01_env` §3.4);
  h_star approach term via the system interface (`approach_barrier`, capability dispatch,
  no name branching) — planar bit-parity pinned.
- M1: q1-q8 green; the omega-convention bug (state omega is already body-frame; obs must
  not rotate it twice) caught by q3, fixed and documented. Suite 156 at M1; 157 at close.
- M2 difficulty (provenance: build-log narrative only — stdout-only, no persisted
  artifact; RCF §8.4): segment-intersect mean 0.4535, frac>=1 0.3335, LQR-only collision
  0.364. M4 triviality gate PASS (0.0200 >= 0.005).
- M3 authority gate (same stdout-only provenance): near-B0 ||L_g V_hat|| median 9.32
  (p05 2.73, n=578), degen 0.0 — PASS; the eps_g/R2 near-B0 sampler is planar-only and was
  inactive for 3D (registered follow-up).
- M5: ~9 h wall-clock; in-loop best 0.9558731 @33000 (= best.pt; the mid-run 0.9376@10500
  was an interim best, superseded — RCF §3/§8.7); advisory gradient watch quiet; no
  collapse; the iter-3-class instability did not recur at bptt_T=30.

## 4. Stage-3D: k=5 fallback SCALING MEASUREMENT (not an adoption; default stays none)

- mode=none reproduces M6 to all digits (zero flips). mode=kstep k=5: cps 0.9586
  [0.94600, 0.96970] (cps CIs OVERLAP M6 by 0.00289); collision 0.0025 [0.0005, 0.0050] —
  within-seed CI separated from M6's [0.0055, 0.0135] by one scene (gap 0.0005 = 1/2000).
- Attribution: 15 fixed / 1 new (net 19 -> 5); ALL flip episodes and all 19 baseline
  collisions are empty-branch episodes (had_empty 1.0) — effect confined to the mechanism's
  domain; fixed episodes were NOT t0-degenerate (median ||L_g|| at t0 1.71; the P4
  degeneracy is at the collision states). Downstream empty steps -22%; chatter -6.8%.
- Scaling reading vs planar (context): 5.9% empty -> ~50% collision reduction (planar);
  1.51% empty -> small absolute effect (-0.0070, 14 net episodes), direction preserved —
  the prevalence-scaling prediction holds. Deployed 3D default REMAINS mode=none
  (per-system evidence rule; this pool lacks adoption-grade power).
- **BINDING CAVEAT — deploy-time cost:** wall-clock 44.4 s -> 941.5 s (21.2x) at |G|=25,
  k=5 (O(|G|^2 k) rollouts on ~1.5% of steps). Research metrics are unaffected (discrete
  time, flag-invariant), but worst-case per-step latency is a deployability limitation;
  mitigation candidates (candidate pruning O(|G| + m|G|), firing-rate reduction) are
  BACKLOG engineering, added to the follow-up list at close (RCF §8.6).

## 5. Mechanism state and carry-forward

Blind-row signature TRANSFERS to 3D: P4 — at the 19 collisions ||L_g V_hat|| median 0.411,
degen_frac 0.316 (population near-B0 median 9.32); P5 empty rate 1.51% (singular 0.99%).
Signal is scarce on this pool (n=19). Approved next axes, sequential: **v2.7.3 = A,
geometry difficulty re-registration** (new pool spec: cylinder density/radius up;
restores statistical power; fallback adoption trial becomes possible there), then
**v2.7.4 = B, full-SO(3) starts** (tilt cap lifted; recovery-stress axis) on A's pool.
Multi-seed escalation: deferred to the post-A headline configuration.

## 6. Caveats and bookkeeping (binding)

1. Provenance grades: M2 difficulty and M3 gate numbers are build-log-narrative-sourced
   (stdout-only); all headline/probe/Stage numbers are artifact-backed. A
   measurement-persistence protocol delta is proposed at step 4 so this class cannot recur.
2. Protocol files 01_env (M0 spec + omega note) and 04_eval (3D-figure follow-up note)
   were modified in-version as intended spec edits; 00/02/03/05/06 untouched (RCF §8.5).
3. Ledger: L128 (M4), L129 (M6), L130 (Stage-3D). At close, L129 becomes the
   quadrotor_3d lineage bold; L130 stays plain (measurement); no cross-lineage bold moves.
4. Observation recorded (not gated): M6 saturation_rate 0.4106 (vs M4 0.0251) — the JT
   policy saturates ~41% of steps; candidate probe for the difficulty axis (RCF §8.8).
5. verify.sh at close: 157 passed. Version strings consistent at v2.7.2.
6. Theory note accompanying this close: cor:obs-3d (flip aliasing in the cylinder class;
   g^b restores exactly the yaw quotient) with its empirical remark, rem:kstep-empirical
   (four-way confirmation incl. this version's prevalence scaling), rem:kstep-commit
   (receding-horizon open item) — compiled, in the canonical tex.